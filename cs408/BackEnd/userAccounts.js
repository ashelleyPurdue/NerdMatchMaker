//TODO: Handle user account stuff.
var sql = require("sql"); //TODO: Figure out how to fix the typescript compile error where it doesn't recognize "require".
//It still produces the right JS output in the meantime, though.
function registerUser(username, hashedPassword) {
    //Registers a user with the given credentials and adds them to the database.
    //Returns true if successful, false if the user already exists.
    return false;
}
